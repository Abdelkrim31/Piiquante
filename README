# P6-test
 
 1 Dans le fichier config est placé le fichier .env : (exemple avec le fichier .env.example)
 
    back
      config
        .env
            
 2 Il faut le fichier node_modules :
  
      Le fichier package.json indique les modules à installer.